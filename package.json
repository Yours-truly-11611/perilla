{
  "name": "perilla",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tinacms dev -c \"hugo server\"",
    "build": "node -e \"console.log('TINA_CLIENT_ID set:', !!process.env.TINA_PUBLIC_CLIENT_ID, '| TINA_TOKEN set:', !!process.env.TINA_TOKEN)\" && tinacms build && hugo --minify"
  },
  "dependencies": {
    "@tinacms/cli": "latest",
    "tinacms": "latest"
  },
  "devDependencies": {
    "@types/node": "^25.3.3"
  }
}
